# PyTorch: pip installs CPU-only by default.
# For GPU (CUDA 12.4), install separately FIRST:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# Then install the rest of this file. If you skip this step, training runs on CPU only.
torch>=2.3
torchvision>=0.18
onnx>=1.16
onnxscript>=0.2
onnxruntime-gpu>=1.18
onnxsim>=0.4.36
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
opencv-python-headless>=4.9
gradio>=4.0
tqdm>=4.66
rich>=13.0
matplotlib>=3.8
pandas>=2.2
faiss-cpu>=1.8
pytest>=8.0
pytest-cov>=5.0
# faiss-gpu>=1.8  # Uncomment if conda build unavailable and you want GPU FAISS
# tensorrt: Must be installed manually from NVIDIA zip package on Windows.
#   See README.md "TensorRT on Windows" section for instructions.
